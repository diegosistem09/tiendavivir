<?php

/**
 * @file
 * Defines plugin for colombian addresses.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function addressfield_co_ctools_plugin_directory($module, $plugin) {
  if ($module == 'addressfield') {
    return 'plugins/' . $plugin;
  }
}
